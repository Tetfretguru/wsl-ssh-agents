# Logs

Each task writes:

- `logs/tasks/<task_id>/events.jsonl`
- `logs/tasks/<task_id>/conversation.md`

`events.jsonl` includes execution metadata per step.
`conversation.md` stores A<->B control messages.
